-- {{name}} — QBCore vehicle config
-- Auto-generated from schemas/generic-rp/{{kebabCase id}}.yaml
-- Edit values; regenerate from YAML for clean reset.

Config = Config or {}

Config.Vehicle = {
    name      = '{{id}}',
    label     = '{{name}}',
    -- Engine layer — bound to game-engine spawn name + handling.meta entry.
    model     = '{{data.engine.model}}',
    handling  = '{{data.engine.handling}}',
    -- Economy layer — pure RP.
    price     = {{data.economy.price}},
    dealer    = '{{data.economy.dealer}}',
    category  = '{{data.economy.category}}',
    tradeable = {{data.economy.tradeable}},
}
